import "./ct1.sol";


function max(uint256 a, uint256 b) pure returns (uint256) {
    return a >= b ? a : b;
}


function adddd(address ad)pure returns (int256){
     VulnerableTokenStore target = VulnerableTokenStore(ad);
     return 0;
}
function max(int256 a, int256 b) pure returns (int256) {
    return a >= b ? a : b;
}
